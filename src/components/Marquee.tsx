import React from "react";

type elemento = {
  url: string,
  name: string,
}

interface Props {
  elementos: elemento | elemento[];
}

export default function Marquee({ elementos }: Props) {
  return <article>
    
  </article>
}